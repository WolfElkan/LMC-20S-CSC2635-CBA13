# CBA 13

To run this project, compile `LinearSearch.java`, and run the resulting `.class` file.  The program will output the program run from the slideshow, and find any user-specified value using a Linear Search.